function toggle() {
    var element = document.getElementById("box");
    element.classList.toggle("open");
    }

    $('.marquee').marquee({
    	duration: 8000,
    	gap: 50,
    	delayBeforeStart: 0,
    	direction: 'right',
    	duplicated: false
    });
